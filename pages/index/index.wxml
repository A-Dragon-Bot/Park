<view class="page">
  <view class="section">
     <text class="txt">使用前记得打开定位服务哦~</text>
  </view>
</view>
<view class="map_container">
  <map class="map" id="map" longitude="{{longitude}}" latitude="{{latitude}}" scale="16" show-location="true" show-compass="true"  enable3D="true" enable-rotate="true" markers="{{markers}}" polyline="{{polyline}}"
    bindmarkertap="makertap" controls="{{controls}}" bindcontroltap="clickcontrol" bindregionchange="mapchange"></map>
</view>
 <view class="map_text" >
   <button bindtap="test">test</button>
   <!-- <button class="test" hover-class='btn-class' disable="{{flag}}" bindtap='getLocation'>手动标定位置</button>

      <button class='map-1' hover-class='btn-class' disabled="{{flag1}}" bindtap="park"> 
        <image src="../imgs/stop1.png"></image>
       <view >停车</view>
      </button>     
    <button class='map-2' hover-class='btn-class' disabled="{{flag2}}" bindtap="isFindcar"> 找到了呢</button>
    <button class='map-3' hover-class='btn-class' disabled="{{flag3}}" bindtap="findCar">
        <image src="../imgs/find.png"></image>
     <view >找车</view>
     </button>   -->
        <!--button bindtap='test'>test</button-->
  <!--<text class="h1">{{textData.name}}</text>
  <text class="h2">{{textData.desc}}</text>-->
</view>
<!--<view class="map_text" wx:else>
  <view class="map-2" wx:if="{{stop}}" bindtap="park">
    <image src="../imgs/stop.png"></image>
    <view wx:if="{{stop}}">停车</view>
   <view class="map-2" bindtap="findCar" wx:else>
    <image src="../imgs/F.png"></image>
     <view >找车</view>
  </view>
 </view>
   <button bindtap='getLocation'>手动标定停车位置</button>
 <text>手动标定停车位置为：\n</text>
 <text class="h1">{{name}}</text>
  <text class="h2">{{address}}</text>
</view>-->
